name = "landlordrealestate"
main = ".open-next/worker.js"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Assets (required for Next.js static files)
[assets]
directory = ".open-next/assets"
binding = "ASSETS"

# Optional: Environment variables
[vars]
MY_VARIABLE = "production"

# Optional: Secrets (better to set these in Cloudflare Dashboard)
# Example: JWT_SECRET, DATABASE_URL, etc.